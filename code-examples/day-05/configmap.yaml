apiVersion: v1
kind: ConfigMap
metadata:
  name: node-app-config
  labels:
    app: node-demo
data:
  PORT: "8080"
  LOG_LEVEL: "info"
  NODE_ENV: "production"
  app.properties: |
    server.port=8080
    server.host=0.0.0.0
    log.level=info
  features.json: |
    {
      "newUI": true,
      "darkMode": false,
      "maxUsers": 1000
    }
